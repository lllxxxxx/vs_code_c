// #include"graph.c"
#include<stdio.h>
// sqQueue creat_queue();
int main(){
   // alGraph g = creat_graph();
   // insertVex(&g);
   // insertVex(&g);
   // insertArc(&g);
   // outPut(&g);
   // printf("||||||||||||||||||||||||||||\n");
   // bfs(&g);
   printf("cs");
}
